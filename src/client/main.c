#include <stdio.h>

#include <proto.h>

/*
 *  -P receive port
 *  -M multipe group
 *  -p play name
 *  -H help
 */
int main()
{

	/* initialize */

	/* receive menulist */

	while (1) {
		if ("is menulist?") {
			break;
		}
	}

	/* select channel */

	while (1) {
		if ("is the chanel you want ?") {
			/* play */
		}
	}


	exit(0);
}
